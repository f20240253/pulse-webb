# render.yaml – Deploy Pulse Streamlit app on Render.com
services:
  - type: web
    name: pulse-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT
